# 📊 可视化仪表盘

这是A股智能分析系统的可视化仪表盘页面。

## 访问地址

如果已启用GitHub Pages，可以通过以下地址访问：

```
https://你的用户名.github.io/daily_stock_analysis/
```

## 数据更新

数据文件 `dashboard_data.json` 会在每次分析完成后自动更新。

## 本地使用

如果需要本地运行，可以使用Flask服务：

```bash
python main.py --webui
```

然后访问: `http://127.0.0.1:8001`
